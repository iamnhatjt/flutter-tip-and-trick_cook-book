// create a class no modifer
// unrestricted to "construct, extend, implemen and mix in";

class Person {
  final String name;
  final int age;

  const Person({
    required this.name,
    required this.age,
  });
}
